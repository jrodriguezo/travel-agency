const BASE_URL = "https://my-json-server.typicode.com";

const VERSION = "/mariosanz92";

export const BASE_URL_VERSION = BASE_URL + VERSION;

export const ENDPOINTS = {
  TRAVELS: "/dream-travels-data/travels",
};
